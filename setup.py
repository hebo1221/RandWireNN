"""Setup script for RandWireNN (for backward compatibility)."""
from setuptools import setup, find_packages

# For modern Python projects, use pyproject.toml instead
# This file is kept for backward compatibility
setup(
    packages=find_packages(),
)
